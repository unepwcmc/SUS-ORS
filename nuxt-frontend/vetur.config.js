module.exports = {
  projects: [
    {
      root: './nuxt-frontend', // root of your vue project (should contain package.json)
      package: './package.json' // Relative to root property, don't change this.
    }
  ]
}
